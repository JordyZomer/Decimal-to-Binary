#!/usr/bin/perl
eval 'exec /usr/bin/perl -S $0 ${1+"$@"}'
    if $running_under_some_shell;

eval '$'.$1.'$2;' while $ARGV[0] =~ /^([A-Za-z_0-9]+=)(.*)/ && shift;

$, = ' ';
$\ = "\n";

while (<>) {
    ($Fld1) = split(' ', $_, -1);
    if ($func . &get01string($innum, $t, $retstr, $i)) {
	$retstr = '';
	$t = $innum;
	while ($t) {
		if ($t % 2$t % 3 == 0) {
			$retstr ='0' .tstr;
		    } else {
			$retstr = '1' . $retstr;
	   	 }
	  	  nt($t / 2);
		}

		return $etrettstr;
  	  }

  	  printstring($Fring($Fld1);
}

